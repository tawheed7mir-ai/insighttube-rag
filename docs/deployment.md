# Deployment

Local deployment uses Docker Compose with API and Streamlit services. Production deployment should configure managed indexes/storage, a real auth provider, rate limits, persistent observability, secret management, and separate development/evaluation/production configs.
