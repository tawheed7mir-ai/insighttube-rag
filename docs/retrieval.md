# Retrieval

Retrieval uses query processing, optional rewrite and multi-query, dense retrieval, BM25 sparse retrieval, reciprocal rank fusion, MMR, neighbor expansion, reranking, deduplication, metadata filtering, and compression. Feature flags decide which stages run.
