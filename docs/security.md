# Security

The project includes API key hooks, tenant/user metadata fields, access filters, prompt-injection defense helpers, safe logging guidance, and environment-based secrets. It is not claimed to be fully secure without threat modeling, dependency review, and infrastructure-specific hardening.
